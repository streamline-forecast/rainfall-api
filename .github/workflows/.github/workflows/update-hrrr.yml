name: Update HRRR Forecast Overlay

on:
  schedule:
    - cron: "25 * * * *"
  workflow_dispatch:

jobs:
  update-hrrr:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install system packages
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            python3 \
            python3-pip \
            gdal-bin \
            libgdal-dev
          python3 --version
          gdal-config --version
          gdal_translate --version

      - name: Install Python dependencies
        run: |
          python3 -m pip install --break-system-packages --upgrade pip
          python3 -m pip install --break-system-packages numpy Pillow boto3
          python3 -m pip install --break-system-packages GDAL==$(gdal-config --version)

      - name: HRRR forecast → PNG + GeoTIFF + GRIB2 → R2
        env:
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET_NAME: ${{ secrets.R2_BUCKET_NAME }}
          R2_PUBLIC_BASE_URL: ${{ secrets.R2_PUBLIC_BASE_URL }}
          R2_ENDPOINT_URL: ${{ secrets.R2_ENDPOINT_URL }}
        run: |
          python3 scripts/hrrr_to_png.py

      - name: Print output URLs
        if: success()
        env:
          R2_PUBLIC_BASE_URL: ${{ secrets.R2_PUBLIC_BASE_URL }}
        run: |
          echo "HRRR Index: ${R2_PUBLIC_BASE_URL}/hrrr/forecast/index.json"
          echo "HRRR F01  : ${R2_PUBLIC_BASE_URL}/hrrr/forecast/png/hrrr_f01.png"

      - name: Report failure
        if: failure()
        run: |
          echo "HRRR forecast processing failed at $(date -u). Check logs above."
